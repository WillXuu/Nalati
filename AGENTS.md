# AGENTS.md

这些规则适用于整个仓库。

## 总体原则
1. 保持改动聚焦于需求本身，避免牵涉无关文件。
2. 优先修复根因而非临时补丁，能复用既有工具就不要重复造轮子。
3. 任何自动化脚本（build/test/dev server）若存在，先阅读其说明再执行或修改。

## 代码风格与结构
1. 保持现有目录结构与命名约定，如需新增模块请遵循已有模式。
2. TypeScript/JavaScript 代码遵循 ESLint 与 Prettier（若有配置）的默认规则；未配置时参照项目内现有写法。
3. 新增或修改的组件需保持无状态/有状态职责清晰，避免引入多余的全局状态。
4. 所有字符串常量优先使用单引号，除非文件内已有统一风格。

## 文档与注释
1. 仅在必要时补充注释，描述“为什么”，而非“做了什么”。
2. 公开接口、复杂逻辑、重要约束需在相邻 README 或代码块上方加入简明说明。

## 测试与验证
1. 若改动影响可测试逻辑，需补充或更新相应测试；若无测试框架，请手动说明验证方式。
2. 在提交前务必本地运行受影响的单元测试或端到端测试，确保通过。

## 沟通与交付
1. 所有回答默认使用中文，且保持简洁、直接、友好。
2. 最终反馈需包含：改动概述、测试/验证情况、潜在后续工作建议。
3. 如果任务遇到阻塞（权限/依赖/信息不足），第一时间说明阻塞点并提出可行解决方案。

遵循以上规则可以确保在本项目中的协作高效且可靠。
